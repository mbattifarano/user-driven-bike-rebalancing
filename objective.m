function [value] = objective(parameters, dstar)
%OBJECTIVE Compute the value of the objevtice function at dstar
%   INPUTS
%       parameters  struct  parameter and data struct
%       dstar       nx1     objective variable
end

